# Instalação local no Windows

Na pasta `apex-Main`:

```bat
npm install
npm run dev
```

O `package-lock.json` desta versão usa somente o registro público oficial do npm (`https://registry.npmjs.org/`).
